<?xml version="1.0" encoding="utf-8"?>
<!-- No menuitem: the CMS sidebar leaf (data/cms_sidebar_analytics.xml) is the
     entry point, and a backend menu root already exists in biz_bi for power
     users. -->
<odoo>

    <record id="action_bi_hub" model="ir.actions.client">
        <field name="name">Analytics</field>
        <field name="tag">biz_bi.hub</field>
    </record>

</odoo>
