<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pricing_engine_form" model="ir.ui.view">
        <field name="name">advanced.pricing.engine.form</field>
        <field name="model">advanced.pricing.engine</field>
        <field name="arch" type="xml">
            <form string="Pricing Engine">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="sequence"/>
                        <field name="active"/>
                        <field name="company_id" groups="base.group_multi_company"/>
                    </group>
                    <group>
                        <field name="enable_multi_level"/>
                        <field name="enable_cascade"/>
                        <field name="cache_duration"/>
                    </group>
                    <notebook>
                        <page string="Rules">
                            <field name="rule_ids"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    
    <record id="view_pricing_engine_list" model="ir.ui.view">
        <field name="name">advanced.pricing.engine.list</field>
        <field name="model">advanced.pricing.engine</field>
        <field name="arch" type="xml">
            <list string="Pricing Engines" decoration-muted="deleted" decoration-danger="deleted">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="active"/>
                <field name="company_id" groups="base.group_multi_company"/>
                            <!-- Record lifecycle: Delete (user) / Archive (custodian) -->
                <field name="deleted" column_invisible="1"/>
                <button name="action_open_delete_wizard" type="object"
                        icon="fa-trash" title="Delete (reason required)"
                        invisible="deleted"/>
                <button name="action_open_restore_wizard" type="object"
                        icon="fa-undo" title="Restore this deleted record"
                        groups="health_base.group_healthcare_custodian"
                        invisible="not deleted"/>
                <button name="action_open_archive_wizard" type="object"
                        icon="fa-archive" title="Archive (custodian)"
                        groups="health_base.group_healthcare_custodian"
                        invisible="deleted"/>
</list>
        </field>
    </record>
</odoo>