<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Healthcare User Administrator group -->
    <record id="group_health_user_admin" model="res.groups">
        <field name="name">Healthcare: User Administrator</field>
        <field name="privilege_id" ref="health_base.res_groups_privilege_healthcare"/>
        <field name="implied_ids" eval="[(4, ref('health_base.group_healthcare_admin'))]"/>
        <field name="comment">Can create and manage users, assign access roles,
and manage role permissions — without access to Settings, 
module installation, or system administration.</field>
    </record>
</odoo>
