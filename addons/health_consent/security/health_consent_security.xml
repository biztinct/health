<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- =====================================================================
         Catchment province record rules (spec §0.3 template) on
         health.consent. The check log carries no catchment scoping:
         rows are created with sudo() by the service API and read
         access is manager+ only (ACL).
         ===================================================================== -->

    <record id="rule_health_consent_catchment" model="ir.rule">
        <field name="name">Consent: Healthcare users see own catchment</field>
        <field name="model_id" ref="model_health_consent"/>
        <field name="domain_force">['&amp;', ('catchment_province_id', '=', user.catchment_province_id.id), ('catchment_province_id', '!=', False)]</field>
        <field name="groups" eval="[(4, ref('health_base.group_healthcare_nurse')), (4, ref('health_base.group_healthcare_head_nurse')), (4, ref('health_base.group_healthcare_doctor')), (4, ref('health_base.group_healthcare_operations_manager')), (4, ref('health_base.group_healthcare_manager'))]"/>
    </record>
    <record id="rule_health_consent_owner" model="ir.rule">
        <field name="name">Consent: Owner sees all</field>
        <field name="model_id" ref="model_health_consent"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('health_base.group_healthcare_owner'))]"/>
    </record>

</odoo>
