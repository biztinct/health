<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Healthcare Convert to Contact Wizard - Customize for healthcare -->
    <record id="view_healthcare_lead2opportunity_partner" model="ir.ui.view">
        <field name="name">crm.lead2opportunity.partner.form.healthcare</field>
        <field name="model">crm.lead2opportunity.partner</field>
        <field name="inherit_id" ref="crm.view_crm_lead2opportunity_partner"/>
        <field name="arch" type="xml">
            <!-- Change form title -->
            <xpath expr="//form" position="attributes">
                <attribute name="string">Convert to Contact</attribute>
            </xpath>
            
            <!-- Change radio button labels for conversion action -->
            <xpath expr="//field[@name='name']" position="replace">
                <field name="name" widget="radio">
                    <option value="convert">Convert to contact</option>
                    <option value="merge">Merge with existing contacts</option>
                </field>
            </xpath>
            
            <!-- Change assign section title -->
            <xpath expr="//group[field[@name='user_id']]" position="attributes">
                <attribute name="string">Assign this contact to</attribute>
            </xpath>
            
            <!-- Change opportunities section title to contacts -->
            <xpath expr="//group[@invisible=&quot;name != 'merge'&quot;]" position="attributes">
                <attribute name="string">Contacts</attribute>
            </xpath>
            
            <!-- Change button text -->
            <xpath expr="//button[@name='action_apply']" position="attributes">
                <attribute name="string">Create Contact</attribute>
            </xpath>
        </field>
    </record>

    <!-- Healthcare Convert to Contact Action - Change name -->
    <record id="action_healthcare_lead2opportunity_partner" model="ir.actions.act_window">
        <field name="name">Convert to Contact</field>
        <field name="res_model">crm.lead2opportunity.partner</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="view_id" ref="view_healthcare_lead2opportunity_partner"/>
    </record>

    <!-- Search View Extension for Contact Status Grouping -->
    <record id="view_healthcare_lead_search" model="ir.ui.view">
        <field name="name">crm.lead.search.healthcare</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.view_crm_case_opportunities_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <field name="catchment_province_id" string="Catchment Area"/>
                <separator string="Catchment Area"/>
                <separator/>
                <!-- The `deleted` reference in-domain disables the implicit
                     deleted_test injection, so these work from any view. -->
                <filter name="filter_deleted" string="Deleted"
                        domain="[('deleted', '=', True)]"/>
                <filter name="filter_not_deleted" string="Not Deleted"
                        domain="[('deleted', '=', False)]"/>
                <filter name="filter_archived_lifecycle" string="Archived"
                        domain="[('active', '=', False)]"/>
            </xpath>
            <xpath expr="//group" position="inside">
                <filter string="Catchment Area" name="groupby_catchment_province" context="{'group_by': 'catchment_province_id'}"/>
                <filter string="Contact Status" name="groupby_contact_status" context="{'group_by': 'contact_status'}"/>
            </xpath>
        </field>
    </record>


    <!-- Add Dashboard and Activity Buttons to Base CRM Lead Form (for calendar popup) -->
    <record id="view_crm_lead_form_dashboard_button" model="ir.ui.view">
        <field name="name">crm.lead.form.dashboard.button</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_open_lead_hub" 
                        string="Dashboard" 
                        type="object" 
                        class="btn-primary"
                        icon="fa-dashboard"
                        title="Open this contact"/>
                <button name="action_execute_activity" 
                        string="Activity" 
                        type="object" 
                        class="btn-secondary"
                        icon="fa-tasks"
                        title="Execute Activity Action (Call/Email/To-Do/Meeting)"
                        invisible="not activity_ids"/>
            </xpath>
        </field>
    </record>

    <!-- Activity View - Hide expected_revenue ($ value) from display and add Back button -->
    <record id="view_healthcare_lead_activity" model="ir.ui.view">
        <field name="name">crm.lead.activity.healthcare</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_activity"/>
        <field name="arch" type="xml">
            <!-- Hide expected_revenue from the activity view -->
            <xpath expr="//field[@name='expected_revenue']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <!-- Action: Open Follow-up Wizard -->
    <record id="action_open_followup_wizard" model="ir.actions.act_window">
        <field name="name">Follow-up Activities</field>
        <field name="res_model">health.followup.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'form_view_initial_mode': 'edit'}</field>
    </record>


    <!-- NEW: Dedicated Healthcare Opportunity Form View (No Inheritance) -->
    <!-- This is the default form view - redirects to hub-spoke dashboard for existing records -->
    <record id="view_healthcare_opportunity_form" model="ir.ui.view">
        <field name="name">crm.lead.form.healthcare.opportunity</field>
        <field name="model">crm.lead</field>
        <field name="priority">1</field>
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <form string="Contact Details" js_class="healthcare_lead_form_redirect">
                <field name="active" invisible="1"/>
                <field name="deleted" invisible="1"/>
                <header>
                    <!-- CONTACT-FIRST ACTION MENU BAR -->
                    
                    <!-- Mark Spam - Marks as junk and returns to dashboard -->
                    <button name="action_mark_spam_and_home"
                            string="Mark Spam"
                            type="object"
                            class="btn-secondary"
                            icon="fa-ban"
                            title="Mark this contact as spam and return to dashboard"
                            confirm="Mark this contact as spam? This action cannot be undone."/>

                    <!-- Record lifecycle: Delete (user) / Archive (custodian) -->
                    <button name="action_open_delete_wizard" type="object"
                            string="Delete" icon="fa-trash" class="btn-secondary"
                            invisible="deleted"/>
                    <button name="action_open_restore_wizard" type="object"
                            string="Restore" icon="fa-undo" class="btn-secondary"
                            groups="health_base.group_healthcare_custodian"
                            invisible="not deleted"/>
                    <button name="action_open_archive_wizard" type="object"
                            string="Archive" icon="fa-archive" class="btn-secondary"
                            groups="health_base.group_healthcare_custodian"
                            invisible="deleted or not active"/>
                    <button name="action_open_unarchive_wizard" type="object"
                            string="Unarchive" icon="fa-archive" class="btn-secondary"
                            groups="health_base.group_healthcare_custodian"
                            invisible="active"/>
                    
                    <!-- Booking - Opens booking creation flow -->
                    <button name="action_convert_to_booking"
                            string="Booking"
                            type="object"
                            class="btn-primary"
                            icon="fa-calendar-plus-o"
                            title="Create a new booking for this contact"
                            invisible="contact_status == 'booking'"/>
                    
                    <!-- Cancellation - Opens cancellation form -->
                    <button name="action_open_cancellation"
                            string="Cancellation"
                            type="object"
                            class="btn-secondary"
                            icon="fa-times-circle"
                            title="Record a cancellation"
                            invisible="contact_status != 'booking'"/>
                    
                    <!-- Reschedule - Opens booking calendar for reschedule -->
                    <button name="action_reschedule_booking"
                            string="Reschedule"
                            type="object"
                            class="btn-secondary"
                            icon="fa-calendar"
                            title="Reschedule the booking"
                            invisible="contact_status != 'booking'"/>
                    
                    <!-- Telemedicine button removed - service type selected in booking wizard -->
                    
                    <!-- Consultation dropdown handled via escalation -->
                    <button name="action_escalate_consultation"
                            string="Consultation"
                            type="object"
                            class="btn-secondary"
                            icon="fa-comments"
                            title="Transfer for consultation"/>
                    
                    <!-- Escalate Contact -->
                    <button name="action_escalate_contact"
                            string="Escalate"
                            type="object"
                            class="btn-secondary"
                            icon="fa-level-up"
                            title="Escalate to Head Nurse or OM"/>
                    
                    <!-- Send Message -->
                    <button name="action_send_message"
                            string="Send Message"
                            type="object"
                            class="btn-secondary"
                            icon="fa-envelope"
                            title="Send message to contact"/>
                    
                    <!-- Log Activity (marks as Lead + opens activity wizard) -->
                    <button name="action_log_as_lead"
                            string="Log Activity"
                            type="object"
                            class="btn-secondary"
                            icon="fa-clock-o"
                            title="Mark as Lead and schedule a follow-up activity"
                            invisible="contact_status == 'lead'"/>
                    
                    <!-- Log Activity -->
                    <button name="action_schedule_follow_up"
                            string="Log Activity"
                            type="object"
                            class="btn-secondary"
                            icon="fa-clock-o"
                            title="Schedule a follow-up activity"/>
                    
                    <!-- Log Note -->
                    <button name="action_log_note"
                            string="Log Note"
                            type="object"
                            class="btn-secondary"
                            icon="fa-sticky-note"
                            title="Add an internal note"/>
                    
                    <!-- Hidden fields for visibility conditions -->
                    <field name="contact_outcome" invisible="1"/>
                    <field name="contact_status" invisible="1"/>
                    
                    <!-- Stage statusbar -->
                    <field name="stage_id" widget="statusbar" options="{'clickable': '1'}" domain="[('team_ids', '=', False)]" invisible="1"/>
                </header>
                
                <sheet string="Contact Details">
                    <!-- Diagonal Ribbon Banner for Booking Lost -->
                    <div invisible="contact_outcome != 'booking_lost'"
                         style="position: absolute; top: 25px; right: -35px; transform: rotate(45deg);
                                background-color: #dc3545; color: white; padding: 5px 40px;
                                font-weight: bold; font-size: 13px; z-index: 999;
                                box-shadow: 0 3px 10px rgba(0,0,0,0.2); text-align: center;">
                        BOOKING LOST
                    </div>

                    <!-- Contact Status + Code Badges -->
                    <div style="position: absolute; top: 10px; right: 100px; z-index: 1000; display: flex; gap: 10px; align-items: center;">
                        <!-- Contact Status Badge -->
                        <span class="badge rounded-pill" style="background-color: #2A7ABF; color: white; font-size: 14px; padding: 8px 18px; font-weight: 600; letter-spacing: 0.5px;" invisible="contact_status != 'active'">
                            Active
                        </span>
                        <span class="badge rounded-pill" style="background-color: #176B47; color: white; font-size: 14px; padding: 8px 18px; font-weight: 600; letter-spacing: 0.5px;" invisible="contact_status != 'booking'">
                            Booking
                        </span>
                        <span class="badge rounded-pill" style="background-color: #946200; color: white; font-size: 14px; padding: 8px 18px; font-weight: 600; letter-spacing: 0.5px;" invisible="contact_status != 'lead'">
                            Lead
                        </span>
                        <span class="badge rounded-pill" style="background-color: #C0332A; color: white; font-size: 14px; padding: 8px 18px; font-weight: 600; letter-spacing: 0.5px;" invisible="contact_status != 'lost_booking'">
                            Lost Booking
                        </span>
                        <span class="badge rounded-pill" style="background-color: #C0332A; color: white; font-size: 14px; padding: 8px 18px; font-weight: 600; letter-spacing: 0.5px;" invisible="contact_status != 'spam'">
                            Spam Call
                        </span>
                        <field name="contact_status" invisible="1"/>

                        <!-- Lifecycle Badges -->
                        <span class="badge rounded-pill vu-lifecycle-badge vu-lifecycle--deleted"
                              invisible="not deleted">
                            <i class="fa fa-trash"/> Deleted
                        </span>
                        <span class="badge rounded-pill vu-lifecycle-badge vu-lifecycle--archived"
                              invisible="active or deleted">
                            <i class="fa fa-archive"/> Archived
                        </span>

                        <!-- Contact ID Badge -->
                        <span class="badge rounded-pill" style="background-color: #E4F4FD; color: #0D356B; font-size: 14px; padding: 8px 18px; font-weight: 700; letter-spacing: 0.5px;">
                            <field name="unique_contact_code" readonly="1" nolabel="1"/>
                        </span>
                    </div>

                    <!-- Smart Buttons -->
                    <div class="oe_button_box" name="button_box">
                        <button name="action_merge_similar_leads"
                                type="object"
                                class="oe_stat_button"
                                icon="fa-clone"
                                invisible="duplicate_lead_count == 0">
                            <field name="duplicate_lead_count" widget="statinfo" string="Similar Leads"/>
                        </button>
                    </div>

                    <!-- Title and Basic Info -->
                    <div class="oe_title">
                        <h1 style="display: flex; align-items: center; gap: 15px;">
                            <field name="name" string="Contact Name" placeholder="Contact name..." required="1"/>
                            <!-- Spam Caller Warning Badge -->
                            <field name="is_spam_caller" invisible="1"/>
                            <field name="contact_status" invisible="1"/>
                            <span class="badge rounded-pill" 
                                  style="background-color: #C0332A; color: white; font-size: 12px; padding: 6px 14px; font-weight: 600; animation: pulse 1.5s infinite;" 
                                  invisible="not is_spam_caller or contact_status == 'booking'">
                                ⚠️ Spam Caller
                            </span>
                        </h1>
                        <!-- Booking Lost Reason Alert -->
                        <div class="alert alert-warning" role="alert" invisible="contact_outcome != 'booking_lost'" style="margin-top: 10px;">
                            <strong>Booking Lost:</strong>
                            <field name="booking_lost_reason" readonly="1" nolabel="1"/>
                        </div>
                    </div>

                    <!-- Relationship Selection -->
                    <div class="vu-card" style="margin-bottom: var(--vu-space-lg);">
                        <div class="vu-card__title">
                            <span class="vu-section__icon" data-icon="users-group"/>
                            <span>Relationship Context</span>
                        </div>
                        <group col="2">
                            <field name="contact_relationship_type" string="I am the:" widget="radio"
                                   options="{'horizontal': true}" style="font-size: 14px; font-weight: normal;"/>
                            <label for="client_name" string="Client Name" invisible="contact_relationship_type == 'client'"/>
                            <div class="o_row" invisible="contact_relationship_type == 'client'">
                                <field name="client_name" nolabel="1" placeholder="Enter client name..."
                                       style="font-size: 14px; font-weight: normal; flex: 1;"
                                       required="contact_relationship_type != 'client'"/>
                                <button name="action_search_client_by_name" type="object" 
                                        class="btn btn-outline-primary btn-sm"
                                        string="🔍 Search" 
                                        title="Search for existing clients"/>
                            </div>
                        </group>
                    </div>
                    
                    
                    <!-- Main Content Tabs -->
                    <notebook>
                        <!-- Contact Details Tab -->
                        <page string="Contact Details" name="contact_details">
                            <div class="vu-section">
                                <div class="vu-section__title">
                                    <span class="vu-section__icon" data-icon="users-group"/>
                                    <div>
                                        <span>Contact Details</span>
                                        <small class="text-muted d-block">Communication channels and classification</small>
                                    </div>
                                </div>
                                <div class="vu-section-grid">
                                    <div class="vu-card">
                                        <div class="vu-card__title">
                                            <span class="vu-section__icon" data-icon="users-group"/>
                                            <span>Contact Information</span>
                                        </div>
                                        <group col="2">
                                            <field name="email_from" string="Email" widget="email"/>
                                            <field name="phone" widget="phone"/>
                                            <field name="phone_duplicate_lead_id" invisible="1"/>
                                            <field name="phone_duplicate_name" invisible="1"/>
                                            <field name="phone_duplicate_code" invisible="1"/>
                                            <field name="contact_datetime" string="Contact Date and Time"/>
                                            <field name="mode_of_contact" string="Mode of Contact"/>
                                            <field name="referrer_id" string="Referrer" options="{'no_create': True}"/>
                                            <field name="emergency_contact_id" string="Emergency Contact" options="{'no_create': True}"/>
                                        </group>
                                        <!-- Duplicate Phone Warning Banner -->
                                        <div class="alert alert-warning d-flex align-items-center justify-content-between" role="alert"
                                             invisible="not phone_duplicate_lead_id"
                                             style="margin: 8px 0; padding: 10px 15px; gap: 12px;">
                                            <div>
                                                <strong>⚠️ Existing contact found with this phone:</strong>
                                                <span class="fw-bold"><field name="phone_duplicate_name" readonly="1" nolabel="1"/></span>
                                                (<field name="phone_duplicate_code" readonly="1" nolabel="1"/>)
                                            </div>
                                            <button name="action_navigate_to_phone_duplicate" type="object"
                                                    class="btn btn-sm btn-primary"
                                                    string="👤 Use Existing Contact"/>
                                        </div>
                                    </div>

                                    <div class="vu-card">
                                        <div class="vu-card__title">
                                            <span class="vu-section__icon" data-icon="heart-pulse"/>
                                            <span>Personal Information</span>
                                        </div>
                                        <group col="2">
                                            <field name="gender"/>
                                            <field name="birth_date"/>
                                            <field name="national_id" string="National ID (CCCD/CMND)"/>
                                            <field name="contact_name" string="Patient Name"/>
                                            <field name="phone2" string="Second Phone"/>
                                            <field name="first_service_date"/>
                                        </group>
                                    </div>

                                    <div class="vu-card">
                                        <div class="vu-card__title">
                                            <span class="vu-section__icon" data-icon="map-pin"/>
                                            <span>Address Details</span>
                                        </div>
                                        <group col="2">
                                            <field name="province_code" string="Province Code"/>
                                            <field name="house_number" string="House Number"/>
                                            <field name="sub_alley_number" string="Sub-Alley Number"/>
                                            <field name="alley_number" string="Alley Number"/>
                                            <field name="ward_commune" string="Ward/Commune"/>
                                            <field name="named_area" string="Named Area"/>
                                            <field name="apartment_number" string="Apartment Number"/>
                                            <field name="building_name" string="Building Name"/>
                                            <field name="zip" string="Postal Code"/>
                                            <field name="street" string="Street" placeholder="Street name / Main road"/>
                                            <!-- City = catchment province; District filtered by it; `city` is derived -->
                                            <field name="catchment_province_name" invisible="1"/>
                                            <field name="city" invisible="1"/>
                                            <field name="catchment_province_id" string="City" options="{'no_create': True}"/>
                                            <field name="district_id" string="District" options="{'no_create': True, 'no_create_edit': True}"/>
                                            <field name="state_id" string="Province" options="{'no_create': True}" placeholder="Select province"/>
                                            <field name="country_id" string="Country" options="{'no_create': True}"/>
                                            <field name="full_vietnamese_address" string="Home Address" colspan="2"/>
                                        </group>
                                    </div>
                                    
                                    <div class="vu-card">
                                        <div class="vu-card__title">
                                            <span class="vu-section__icon" data-icon="tag"/>
                                            <span>Contact Classification</span>
                                        </div>
                                        <group col="2">
                                            <field name="contact_type" string="Contact Type"/>
                                            <field name="contact_reason_id" string="Contact Reason" options="{'no_create': True}"/>
                                            <field name="service_interest" string="Service Interest"/>
                                            <field name="reason_if_rejected" string="Reason if Rejected" invisible="stage_id != 'lost'"/>
                                        </group>
                                    </div>
                                </div>
                            </div>
                        </page>
                        
                        <!-- Source and Campaign Tab -->
                        <page string="Source and Campaign" name="source_campaign">
                            <div class="vu-section">
                                <div class="vu-section__title">
                                    <span class="vu-section__icon" data-icon="timeline"/>
                                    <div>
                                        <span>Source Tracking</span>
                                        <small class="text-muted d-block">Marketing attribution and campaign context</small>
                                    </div>
                                </div>
                                <div class="vu-card">
                                    <group col="2">
                                        <field name="source_id" string="Source" options="{'no_create': True}"/>
                                        <field name="medium_id" string="Medium" options="{'no_create': True}"/>
                                        <field name="campaign_id" string="Campaign" options="{'no_create': True}"/>
                                    </group>
                                </div>
                            </div>
                        </page>
                        
                        <!-- Service and Booking Tab -->
                        <page string="Service and Booking" name="service_booking">
                            <div class="vu-section">
                                <div class="vu-section__title">
                                    <span class="vu-section__icon" data-icon="heart-pulse"/>
                                    <div>
                                        <span>Service and Booking</span>
                                        <small class="text-muted d-block">Interest captured and follow-up plan</small>
                                    </div>
                                </div>
                                <div class="vu-section-grid">
                                    <div class="vu-card">
                                        <div class="vu-card__title">
                                            <span class="vu-section__icon" data-icon="file-text"/>
                                            <span>Service Information</span>
                                        </div>
                                        <group col="2">
                                            <field name="lead_reason_id" string="Lead Reason" options="{'no_create': True}"/>
                                        </group>
                                    </div>

                                    <div class="vu-card">
                                        <div class="vu-card__title">
                                            <span class="vu-section__icon" data-icon="timeline"/>
                                            <span>Follow-up Management</span>
                                        </div>
                                        <group col="2">
                                            <field name="follow_up_count" string="Follow-up Count" readonly="1"/>
                                            <field name="next_follow_up_date" string="Next Follow-up Date"/>
                                            <field name="lead_followup_required" string="Follow-up Required"/>
                                            <field name="follow_up_notes" string="Follow-up Notes" colspan="2"/>
                                        </group>
                                    </div>
                                </div>
                            </div>
                        </page>

                        <!-- Analytics Tab -->
                        <page string="Analytics and Tracking" name="analytics" invisible="1">
                            <div class="vu-section">
                                <div class="vu-section__title">
                                    <span class="vu-section__icon" data-icon="timeline"/>
                                    <div>
                                        <span>Analytics and Tracking</span>
                                        <small class="text-muted d-block">Lifecycle timing metrics</small>
                                    </div>
                                </div>
                                <div class="vu-card">
                                    <group col="2">
                                        <field name="day_open" string="Days Open" readonly="1"/>
                                        <field name="day_close" string="Days to Close" readonly="1"/>
                                        <field name="create_date" string="Created On" readonly="1"/>
                                        <field name="date_closed" string="Closed On" readonly="1"/>
                                    </group>
                                </div>
                            </div>
                        </page>

                        <!-- Notes Tab -->
                        <page string="Internal Notes" name="notes">
                            <div class="vu-card">
                                <div class="vu-card__title">
                                    <span class="vu-section__icon" data-icon="file-text"/>
                                    <span>Internal Notes</span>
                                </div>
                                <group>
                                    <field name="description" string="Internal Notes" nolabel="1"
                                           placeholder="Internal notes about this healthcare contact..."/>
                                </group>
                            </div>
                        </page>
                    </notebook>

                    <!-- Contact History Timeline -->
                    <widget name="contact_timeline"/>
                </sheet>
                
                <chatter reload_on_follower="True"/>
            </form>
        </field>
    </record>

    <!-- Healthcare Opportunities Action - Uses dedicated form for type=opportunity -->
    <record id="action_healthcare_opportunities" model="ir.actions.act_window">
        <field name="name">CRM Contacts</field>
        <field name="res_model">crm.lead</field>
        <field name="view_mode">kanban,list,form,calendar,pivot,graph,activity</field>
        <field name="domain">[('type', '=', 'opportunity'), '|', ('deleted', '=', False), ('deleted', '=', True)]</field>
        <field name="context">{
            'default_type': 'opportunity',
            'form_view_ref': 'health_crm.view_healthcare_opportunity_form',
            'deleted_test': False,
        }</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'kanban'}),
            (0, 0, {'view_mode': 'list'}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_healthcare_opportunity_form')}),
            (0, 0, {'view_mode': 'calendar'}),
            (0, 0, {'view_mode': 'pivot'}),
            (0, 0, {'view_mode': 'graph'}),
            (0, 0, {'view_mode': 'activity'}),
        ]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first healthcare contact!
            </p><p>
                A healthcare contact is a potential patient, caregiver, or payer who has shown interest in your services.
                Use this section to manage your healthcare relationships and track your interactions.
            </p>
        </field>
    </record>

    <!-- All Contacts Action - List view grouped by contact_status -->
    <record id="action_all_contacts_grouped" model="ir.actions.act_window">
        <field name="name">All Contacts</field>
        <field name="res_model">crm.lead</field>
        <field name="view_mode">list,form,kanban,calendar,pivot,graph,activity</field>
        <field name="domain">[('type', '=', 'opportunity')]</field>
        <field name="context">{
            'default_type': 'opportunity',
            'search_default_groupby_contact_status': 1,
            'form_view_ref': 'health_crm.view_healthcare_opportunity_form',
        }</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list'}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_healthcare_opportunity_form')}),
            (0, 0, {'view_mode': 'kanban'}),
            (0, 0, {'view_mode': 'calendar'}),
            (0, 0, {'view_mode': 'pivot'}),
            (0, 0, {'view_mode': 'graph'}),
            (0, 0, {'view_mode': 'activity'}),
        ]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No contacts found!
            </p><p>
                Use this view to see all contacts grouped by their status.
            </p>
        </field>
    </record>

    <!-- Healthcare Lead Form - Change Convert button label for leads -->
    <record id="view_healthcare_lead_form_button" model="ir.ui.view">
        <field name="name">crm.lead.form.healthcare.button</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">
            <!-- Change "Convert to Opportunity" button text to "Convert to Contact" and use our custom action -->
            <xpath expr="//button[@name='%(crm.action_crm_lead2opportunity_partner)d']" position="attributes">
                <attribute name="string">Convert to Contact</attribute>
                <attribute name="name">%(action_healthcare_lead2opportunity_partner)d</attribute>
            </xpath>
        </field>
    </record>

    <!-- Healthcare Lead List View Extension - Change name field label -->
    <record id="view_healthcare_lead_tree" model="ir.ui.view">
        <field name="name">crm.lead.tree.healthcare</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_tree_view_leads"/>
        <field name="arch" type="xml">
            <!-- Change field label 'name' to 'Contact name' in list view -->
            <xpath expr="//field[@name='name']" position="attributes">
                <attribute name="string">Contact name</attribute>
            </xpath>
            <!-- Replace contact_name with client_name to show actual client -->
            <xpath expr="//field[@name='contact_name']" position="replace">
                <field name="client_name" string="Client Name"/>
            </xpath>
            <!-- Record lifecycle: Delete is a user function, Archive a
                 custodian function (client data-governance spec). -->
            <xpath expr="//list" position="inside">
                <field name="deleted" column_invisible="1"/>
                <button name="action_open_delete_wizard" type="object"
                        icon="fa-trash" title="Delete (reason required)"
                        invisible="deleted"/>
                <button name="action_open_restore_wizard" type="object"
                        icon="fa-undo" title="Restore this deleted record"
                        groups="health_base.group_healthcare_custodian"
                        invisible="not deleted"/>
                <button name="action_open_archive_wizard" type="object"
                        icon="fa-archive" title="Archive (custodian)"
                        groups="health_base.group_healthcare_custodian"
                        invisible="deleted"/>
            </xpath>
        </field>
    </record>

    <!-- Healthcare Lead Kanban View Extension - Change name field label and remove partner_id -->
    <record id="view_healthcare_lead_kanban" model="ir.ui.view">
        <field name="name">crm.lead.kanban.healthcare</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_kanban_view_leads"/>
        <field name="arch" type="xml">
            <!-- Set quick_create_view to use our custom form -->
            <xpath expr="//kanban" position="attributes">
                <attribute name="quick_create_view">health_crm.view_healthcare_crm_lead_quick_create</attribute>
            </xpath>

            <!-- Change field label 'name' to 'Contact name' in kanban view -->
            <xpath expr="//field[@name='name']" position="attributes">
                <attribute name="string">Contact name</attribute>
            </xpath>

            <!-- Remove partner_id field from kanban view -->
            <xpath expr="//field[@name='partner_id' and @class='text-truncate']" position="replace"/>
        </field>
    </record>

    <!-- Healthcare Opportunities List View Extension - Fix column labels -->
    <record id="view_healthcare_opportunity_tree" model="ir.ui.view">
        <field name="name">crm.lead.tree.opportunities.healthcare</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_case_tree_view_oppor"/>
        <field name="arch" type="xml">
            <!-- Lifecycle: Delete requests stay visible, struck in red -->
            <xpath expr="//list" position="attributes">
                <attribute name="decoration-danger">deleted</attribute>
                <attribute name="decoration-muted">deleted</attribute>
            </xpath>
            <xpath expr="//list" position="inside">
                <field name="deleted" column_invisible="1"/>
                <button name="action_open_delete_wizard" type="object"
                        icon="fa-trash" title="Delete (reason required)"
                        invisible="deleted"/>
                <button name="action_open_restore_wizard" type="object"
                        icon="fa-undo" title="Restore this deleted record"
                        groups="health_base.group_healthcare_custodian"
                        invisible="not deleted"/>
                <button name="action_open_archive_wizard" type="object"
                        icon="fa-archive" title="Archive (custodian)"
                        groups="health_base.group_healthcare_custodian"
                        invisible="deleted"/>
            </xpath>
            <!-- 'name' field is the caller/contact person, NOT the client -->
            <xpath expr="//field[@name='name']" position="attributes">
                <attribute name="string">Contact Name</attribute>
            </xpath>
            <!-- Replace contact_name with client_name (actual client/patient) -->
            <xpath expr="//field[@name='contact_name']" position="replace">
                <field name="client_name" string="Client Name"/>
            </xpath>
            <!-- Replace stage_id with contact_status -->
            <xpath expr="//field[@name='stage_id']" position="replace">
                <field name="contact_status"/>
                <field name="activity_date_deadline" string="Activity Date" widget="remaining_days"/>
            </xpath>
            <!-- Hide expected_revenue column - not relevant for healthcare -->
            <xpath expr="//field[@name='expected_revenue']" position="attributes">
                <attribute name="column_invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <!-- Healthcare Quick Create Form - ONLY for quick create in kanban, NOT for opening records -->
    <record id="view_healthcare_crm_lead_quick_create" model="ir.ui.view">
        <field name="name">crm.lead.form.quick.healthcare</field>
        <field name="model">crm.lead</field>
        <!-- No mode="primary" - this prevents it from being used as default form -->
        <field name="priority">999</field>
        <field name="arch" type="xml">
            <form string="Quick Create Contact">
                <group>
                    <field name="type" invisible="1"/>
                    <field name="name" string="Contact Name" placeholder="e.g. John Doe"/>
                    <field name="catchment_province_id" string="Catchment Province" options="{'no_create': True}"/>
                    <field name="email_from" string="Email" placeholder="e.g. &quot;email@address.com&quot;"/>
                    <field name="phone" placeholder="e.g. &quot;0123456789&quot;"/>
                </group>
            </form>
        </field>
    </record>

    <!-- ============================================================================
         HIDE GENERATE LEADS BUTTON (from crm_iap_mine module)
         ============================================================================ -->
    
    <!-- Hide Generate Leads button from opportunities tree view -->
    <record id="view_hide_generate_leads_tree_oppor" model="ir.ui.view">
        <field name="name">crm.lead.tree.oppor.hide.generate.leads</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm_iap_mine.crm_lead_view_tree_opportunity"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_generate_leads']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>
    
    <!-- Hide Generate Leads button from leads tree view -->
    <record id="view_hide_generate_leads_tree_leads" model="ir.ui.view">
        <field name="name">crm.lead.tree.leads.hide.generate.leads</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm_iap_mine.crm_lead_view_tree_lead"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_generate_leads']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>
    
    <!-- Hide Generate Leads button from kanban view -->
    <record id="view_hide_generate_leads_kanban" model="ir.ui.view">
        <field name="name">crm.lead.kanban.hide.generate.leads</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm_iap_mine.view_crm_lead_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_generate_leads']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>
    
    <!-- Hide Generate Leads button from leads kanban view -->
    <record id="view_hide_generate_leads_kanban_leads" model="ir.ui.view">
        <field name="name">crm.lead.kanban.leads.hide.generate.leads</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm_iap_mine.crm_case_kanban_view_leads"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_generate_leads']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

    <!-- ================================================================= -->
    <!-- RECENT CLIENT FOLLOW-UP LIST VIEW                                 -->
    <!-- ================================================================= -->
    
    <!-- Custom List View for Recent Client Follow-up (leads with booking) -->
    <record id="view_crm_lead_recent_followup_tree" model="ir.ui.view">
        <field name="name">crm.lead.recent.followup.tree</field>
        <field name="model">crm.lead</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <list string="Recent Client Follow-up" decoration-success="booking_within_30_days">
                <field name="name" string="Client Name"/>
                <field name="phone" widget="phone"/>
                <field name="email_from" string="Email"/>
                <field name="last_booking_date" string="Last Booking"/>
                <field name="booking_within_30_days" column_invisible="1"/>
                <field name="activity_date_deadline" string="Next Activity" widget="remaining_days"/>
                <field name="user_id" string="Salesperson" widget="many2one_avatar_user"/>
                <field name="contact_status" column_invisible="1"/>
                <!-- Activity button to log follow-up -->
                <button name="action_schedule_activity_wizard" 
                        type="object" 
                        icon="fa-clock-o" 
                        title="Log Activity"
                        class="btn-link"/>
            </list>
        </field>
    </record>
    
    <!-- ================================================================= -->
    <!-- FOLLOW-UP CALENDAR VIEW                                          -->
    <!-- ================================================================= -->
    
    <!-- Custom Calendar View for Follow-up Activities -->
    <record id="view_crm_lead_followup_calendar" model="ir.ui.view">
        <field name="name">crm.lead.followup.calendar</field>
        <field name="model">crm.lead</field>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <calendar string="Follow-up Calendar"
                      date_start="calendar_date"
                      date_stop="calendar_date_end"
                      all_day="calendar_all_day"
                      color="contact_status"
                      mode="month"
                      quick_create="false"
                      event_open_popup="true"
                      event_limit="5">
                <!-- Display name (shown as calendar entry title) -->
                <field name="calendar_display_name" string="Activity"/>
                
                <!-- Sidebar filter: Contact Status with checkboxes -->
                <field name="contact_status" filters="1"/>
                
                <!-- Sidebar filter: Salesperson with checkboxes -->
                <field name="user_id" string="Salesperson" filters="1" 
                       widget="many2one_avatar_user" avatar_field="avatar_128"/>

                <field name="catchment_province_id" string="Catchment Area" filters="1"/>
                
                <!-- Phone for Zalo calling (shown in popover) -->
                <field name="phone" widget="phone"/>
                
                <!-- Activity type for action buttons - stored on most recent activity -->
                <field name="activity_type_id" invisible="1"/>
            </calendar>
        </field>
    </record>
    
    <!-- Search View for Follow-up Calendar -->
    <record id="view_crm_lead_followup_calendar_search" model="ir.ui.view">
        <field name="name">crm.lead.followup.calendar.search</field>
        <field name="model">crm.lead</field>
        <field name="priority">50</field>
        <field name="arch" type="xml">
            <search string="Search Follow-ups">
                <!-- Search Fields -->
                <field name="name" string="Contact Name" filter_domain="[('name', 'ilike', self)]"/>
                <field name="phone"/>
                <field name="user_id" string="Salesperson"/>
                <field name="contact_status"/>
                <field name="catchment_province_id" string="Catchment Area"/>

                <separator string="Catchment Area"/>
                
                <!-- Status filters -->
                <filter string="Initial Contact" name="filter_active" 
                        domain="[('contact_status', '=', 'active')]"/>
                <filter string="Lead" name="filter_lead" 
                        domain="[('contact_status', '=', 'lead')]"/>
                <filter string="Booking" name="filter_booking" 
                        domain="[('contact_status', '=', 'booking')]"/>
                <filter string="Lost Booking" name="filter_lost" 
                        domain="[('contact_status', '=', 'lost_booking')]"/>
                
                <separator/>
                
                <!-- Activity filters -->
                <filter string="Has Activities" name="filter_has_activities" 
                        domain="[('activity_ids', '!=', False)]"/>
                <filter string="Overdue" name="filter_overdue" 
                        domain="[('activity_date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Today" name="filter_today" 
                        domain="[('activity_date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Future" name="filter_future" 
                        domain="[('activity_date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                
                <separator/>
                
                <!-- Group By -->
                <filter string="Contact Status" name="group_contact_status" 
                        context="{'group_by': 'contact_status'}"/>
                <filter string="Salesperson" name="group_user" 
                        context="{'group_by': 'user_id'}"/>
                <filter string="Catchment Area" name="group_catchment_province"
                        context="{'group_by': 'catchment_province_id'}"/>
            </search>
        </field>
    </record>
    
    <!-- Action for Follow-up Calendar from Wizard -->
    <record id="action_followup_calendar" model="ir.actions.act_window">
        <field name="name">Follow-up Calendar</field>
        <field name="res_model">crm.lead</field>
        <field name="view_mode">calendar,list,form</field>
        <field name="domain">[('calendar_date', '!=', False)]</field>
        <field name="context">{}</field>
        <field name="search_view_id" ref="view_crm_lead_followup_calendar_search"/>
        <field name="view_ids" eval="[
            (5, 0, 0),
            (0, 0, {'view_mode': 'calendar', 'view_id': ref('view_crm_lead_followup_calendar')}),
            (0, 0, {'view_mode': 'list'}),
            (0, 0, {'view_mode': 'form'}),
        ]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No follow-up activities scheduled
            </p>
            <p>
                Leads and contacts with scheduled activities will appear here.
                <br/>
                <strong>Color Legend:</strong>
                <ul>
                    <li><span style="color: #C0332A;">●</span> Red = Overdue activities</li>
                    <li><span style="color: #ea580c;">●</span> Orange = Due today</li>
                    <li><span style="color: #946200;">●</span> Amber = Lead needs attention</li>
                    <li><span style="color: #2A7ABF;">●</span> Blue = Future activities</li>
                    <li><span style="color: #176B47;">●</span> Green = Booking</li>
                    <li><span style="color: #7c3aed;">●</span> Purple = New/Active</li>
                </ul>
            </p>
        </field>
    </record>


</odoo>
