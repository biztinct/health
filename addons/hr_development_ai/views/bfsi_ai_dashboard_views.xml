<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Business Impact Dashboard — deprecated alias, re-tagged into the
         shell (Analytics tab) so stale bookmarks keep working -->
    <record id="action_bfsi_ai_dashboard" model="ir.actions.client">
        <field name="name">Business Impact Dashboard</field>
        <field name="tag">bfsi_coaching_workspace</field>
        <field name="target">main</field>
        <field name="params">{'start': 'analytics'}</field>
    </record>

</odoo>
